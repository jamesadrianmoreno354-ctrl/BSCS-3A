// Minimal client enhancements: fetch JSON for SPA-like behavior (optional)
(function () {
  // If JS wants to fetch and render dynamically, keep code here.
  // Currently the main listing is server-rendered for SEO.
})();
